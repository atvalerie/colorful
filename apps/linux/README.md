# Linux shell

First executable target: Rust with GTK/libadwaita and a replaceable GStreamer
playback adapter. The initial vertical slice is sign in, search, play, pause,
seek, queue next, and expose an MPRIS media session.

